*.iml
.kotlin
.gradle
**/build/
xcuserdata
!src/**/build/
local.properties
.idea
.DS_Store
captures
.externalNativeBuild
.cxx
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
**/xcshareddata/WorkspaceSettings.xcsettings
node_modules/

# Cursor
.cursor/

# BuildConfig (contains API keys)
**/config/BuildConfig.kt

# Firebase project config. Not a credential — security comes from Firebase
# rules — but on a public repository it lets anyone point a build at this
# project and spend its free-tier quota. See README for local setup.
google-services.json
GoogleService-Info.plist

# Firebase CLI deploy cache — local only, regenerated on every deploy.
.firebase/
firebase-debug.log

# dual-code workflow artifacts
.ai-dual-code/

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ requires this to be granted at runtime; see MainActivity. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        allowBackup=false: with it on, anything the app stores can be pulled off
        the device through the backup transport without unlocking it. Nothing
        here is secret, but the reader's saved articles and language are still
        theirs, and the setting costs nothing to get right.

        No usesCleartextTraffic: the backend is https-only. Leaving the flag on
        would let a network attacker downgrade any future request to http.
    -->
    <application
        android:name=".NewsShortsApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewsShorts">
        <!--
            singleTask so a notification tap or a deep link reuses the running
            instance and reaches onNewIntent. With the default mode Android
            would start a second copy and onNewIntent would never fire.
        -->
        <activity
            android:exported="true"
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.NewsShorts"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="newsshorts" android:host="article" />
            </intent-filter>

            <!--
                A shared link opens the app directly instead of bouncing through
                a browser. autoVerify makes Android fetch
                https://${shareLinkHost}/.well-known/assetlinks.json at install
                time and, when the signing fingerprint matches, hand these links
                straight here with no chooser. Without verification the same
                link still works — it just lands on the web page instead.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="${shareLinkHost}"
                    android:pathPrefix="${shareLinkPathPrefix}" />
            </intent-filter>

            <!--
                The passwordless sign-in link. Firebase serves the assetlinks
                file for this host itself, so verification needs nothing from us
                beyond the SHA-256 fingerprint being registered in the console —
                which is also what makes the link open here rather than in a
                browser tab where the app could never complete the sign-in.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="${firebaseAuthLinkHost}"
                    android:pathPrefix="/__/auth/links" />
            </intent-filter>
        </activity>

        <service
            android:name=".notifications.NewsMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>
